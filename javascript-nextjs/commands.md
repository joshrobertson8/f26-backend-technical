# Commands

Run the installer in `setup/` from the repository root and select JavaScript (Next.js). Setup opens this folder. Run tests after editing the service.
Tests use the latest code and start and stop the server automatically.

```sh
npm test
```

Check the scaffold only:

